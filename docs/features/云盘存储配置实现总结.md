# 云盘存储配置实现总结

## 概述

本实现完成了云盘三种存储方式（本地存储、阿里云OSS、腾讯云COS）的支持，并且实现了从数据库读取存储配置的功能，支持公司级独立配置。

## 已完成的功能

### 1. 存储实现架构

#### 统一存储接口 (`internal/pkg/storage/storage.go`)
- 定义了完整的存储操作接口 `Storage`
- 支持上传、下载、删除、查询等核心功能
- 支持分块上传和预签名URL生成
- 提供对象信息查询和列举功能

#### 存储管理器 (`internal/pkg/storage/manager.go`)
- 负责管理多种存储实现
- 支持通过配置文件初始化存储实例
- 自动处理配置验证和错误恢复
- 提供默认存储和指定类型存储的获取方式

#### 存储实现

**本地存储** (`internal/pkg/storage/local_storage.go`)
- 文件存储在服务器本地磁盘
- 支持文件管理和路径操作
- 不支持预签名URL和分块上传

**阿里云OSS存储** (`internal/pkg/storage/aliyun_oss.go`)
- 使用阿里云对象存储服务
- 支持完整的API操作
- 支持CDN加速和分块上传（待完善）

**腾讯云COS存储** (`internal/pkg/storage/tencent_cos.go`)
- 使用腾讯云对象存储服务
- 支持完整的API操作
- 支持CDN加速和分块上传
- 已实现原生分块上传功能

### 2. 公司级存储管理器 (`internal/pkg/storage/company_manager.go`)

#### 核心功能
- 从数据库读取存储配置（支持缓存）
- 公司级配置与全局配置的融合
- 配置缓存机制（5分钟TTL）
- 配置变更时的自动刷新

#### 配置优先级
系统采用以下配置优先级（从高到低）：

1. **数据库公司级配置**：`cloud_storage_config` 表中对应公司的配置（最高优先级）
2. **配置文件配置**：`config.yaml` 中的 `cloud.storage` 配置（备用方案）
3. **系统默认值**：代码中定义的默认值（兜底方案）

#### 主要方法
```go
// 初始化公司级存储管理器
func NewCompanyStorageManager(
	globalConfig *config.StorageConfig,
	configRepo repository.CloudStorageConfigRepository,
	defaultCompanyID uint,
) (*CompanyStorageManager, error)

// 获取指定公司的存储实例（带缓存）
func (m *CompanyStorageManager) GetStorage(ctx context.Context, companyID uint) (Storage, error)

// 获取默认存储（全局存储）
func (m *CompanyStorageManager) GetDefaultStorage() (Storage, error)

// 获取公司的存储配置
func (m *CompanyStorageManager) GetConfig(ctx context.Context, companyID uint) (*entity.CloudStorageConfig, error)

// 检查公司是否有配置
func (m *CompanyStorageManager) HasCompanyConfig(ctx context.Context, companyID uint) bool

// 刷新公司存储配置缓存
func (m *CompanyStorageManager) RefreshCompanyConfig(ctx context.Context, companyID uint) error

// 清除所有缓存
func (m *CompanyStorageManager) ClearAllCache()
```

### 3. 数据库配置支持

#### 实体模型 (`internal/model/entity/cloud_storage_config.go`)
```go
type CloudStorageConfig struct {
	ID                    uint       `gorm:"column:ID;primaryKey" json:"id"`
	SysCompanyID          uint       `gorm:"column:SYS_COMPANY_ID;not null;index" json:"sysCompanyId"`
	StorageType           string     `gorm:"column:STORAGE_TYPE;size:20;default:local" json:"storageType"`

	// 本地存储配置
	LocalBasePath         string     `gorm:"column:LOCAL_BASE_PATH;size:500" json:"localBasePath"`
	LocalBaseURL          string     `gorm:"column:LOCAL_BASE_URL;size:500" json:"localBaseURL"`

	// 阿里云OSS配置
	AliyunOSSEndpoint     string     `gorm:"column:ALIYUN_OSS_ENDPOINT;size:255" json:"aliyunOssEndpoint"`
	AliyunOSSAccessKeyID  string     `gorm:"column:ALIYUN_OSS_ACCESS_KEY_ID;size:255" json:"aliyunOssAccessKeyId"`
	AliyunOSSAccessKeySecret string  `gorm:"column:ALIYUN_OSS_ACCESS_KEY_SECRET;size:255" json:"aliyunOssAccessKeySecret"`
	AliyunOSSBucketName   string     `gorm:"column:ALIYUN_OSS_BUCKET_NAME;size:255" json:"aliyunOssBucketName"`
	AliyunOSSCDNDomain    string     `gorm:"column:ALIYUN_OSS_CDN_DOMAIN;size:500" json:"aliyunOssCdnDomain"`

	// 腾讯云COS配置
	TencentCOSBucketURL   string     `gorm:"column:TENCENT_COS_BUCKET_URL;size:500" json:"tencentCosBucketUrl"`
	TencentCOSSecretID    string     `gorm:"column:TENCENT_COS_SECRET_ID;size:255" json:"tencentCosSecretId"`
	TencentCOSSecretKey   string     `gorm:"column:TENCENT_COS_SECRET_KEY;size:255" json:"tencentCosSecretKey"`
	TencentCOSBucketName  string     `gorm:"column:TENCENT_COS_BUCKET_NAME;size:255" json:"tencentCosBucketName"`
	TencentCOSRegion      string     `gorm:"column:TENCENT_COS_REGION;size:50" json:"tencentCosRegion"`
	TencentCOSCDNDomain   string     `gorm:"column:TENCENT_COS_CDN_DOMAIN;size:500" json:"tencentCosCdnDomain"`

	IsActive              string     `gorm:"column:IS_ACTIVE;size:1;default:Y" json:"isActive"`
	CreateBy              string     `gorm:"column:CREATE_BY;size:80" json:"createBy"`
	CreateTime            time.Time  `gorm:"column:CREATE_TIME;autoCreateTime" json:"createTime"`
	UpdateBy              string     `gorm:"column:UPDATE_BY;size:80" json:"updateBy"`
	UpdateTime            time.Time  `gorm:"column:UPDATE_TIME;autoUpdateTime" json:"updateTime"`
}
```

#### 仓储接口 (`internal/repository/cloud_storage_config_repository.go`)
```go
type CloudStorageConfigRepository interface {
	GetByCompanyID(ctx context.Context, sysCompanyID uint) (*entity.CloudStorageConfig, error)
	GetAllActiveConfigs(ctx context.Context) ([]*entity.CloudStorageConfig, error)
	GetDefaultConfig(ctx context.Context) (*entity.CloudStorageConfig, error)
	Create(ctx context.Context, config *entity.CloudStorageConfig) error
	Update(ctx context.Context, config *entity.CloudStorageConfig) error
	Delete(ctx context.Context, id uint) error
	GetCount(ctx context.Context) (int64, error)
	Transaction(ctx context.Context, fn func(tx *gorm.DB) error) error
}
```

#### MySQL实现 (`internal/repository/mysql/cloud_storage_config_repository.go`)
- 完整的CRUD操作实现
- 支持按公司ID查询配置
- 支持查询所有有效配置
- 支持事务操作

### 4. 数据库迁移脚本 (`sqls/migrations/add_cloud_storage_config_table.sql`)

创建了 `cloud_storage_config` 表，并为默认公司（ID=1）插入了默认配置。

## 配置说明

### 配置文件结构 (`configs/config.yaml`)

```yaml
storage:
  # 默认存储类型: local, aliyunOSS, tencentCOS
  default: "local"

  local:
    basePath: "uploads"    # 本地存储基础路径
    baseURL: "/files"       # 本地存储访问URL

  aliyunOSS:
    endpoint: ""            # OSS区域
    accessKeyID: ""         # Access Key ID
    accessKeySecret: ""     # Access Key Secret
    bucketName: ""          # 存储空间名称
    cdnDomain: ""           # CDN域名（可选）

  tencentCOS:
    bucketURL: ""           # COS Bucket URL
    secretID: ""            # Secret ID
    secretKey: ""           # Secret Key
    bucketName: ""          # 存储空间名称
    region: ""              # 区域
    cdnDomain: ""           # CDN域名（可选）
```

### 数据库配置表结构

```sql
CREATE TABLE `cloud_storage_config` (
    `ID` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    `SYS_COMPANY_ID` INT UNSIGNED NOT NULL COMMENT '公司ID',
    `STORAGE_TYPE` VARCHAR(20) NOT NULL DEFAULT 'local' COMMENT '存储类型: local, aliyunOSS, tencentCOS',

    -- 本地存储配置
    `LOCAL_BASE_PATH` VARCHAR(500) DEFAULT 'uploads' COMMENT '本地存储基础路径',
    `LOCAL_BASE_URL` VARCHAR(500) DEFAULT '/files' COMMENT '本地存储基础URL',

    -- 阿里云OSS配置
    `ALIYUN_OSS_ENDPOINT` VARCHAR(255) COMMENT '阿里云OSS Endpoint',
    `ALIYUN_OSS_ACCESS_KEY_ID` VARCHAR(255) COMMENT '阿里云OSS AccessKeyID',
    `ALIYUN_OSS_ACCESS_KEY_SECRET` VARCHAR(255) COMMENT '阿里云OSS AccessKeySecret',
    `ALIYUN_OSS_BUCKET_NAME` VARCHAR(255) COMMENT '阿里云OSS Bucket名称',
    `ALIYUN_OSS_CDN_DOMAIN` VARCHAR(500) COMMENT '阿里云OSS CDN域名',

    -- 腾讯云COS配置
    `TENCENT_COS_BUCKET_URL` VARCHAR(500) COMMENT '腾讯云COS Bucket URL',
    `TENCENT_COS_SECRET_ID` VARCHAR(255) COMMENT '腾讯云COS SecretID',
    `TENCENT_COS_SECRET_KEY` VARCHAR(255) COMMENT '腾讯云COS SecretKey',
    `TENCENT_COS_BUCKET_NAME` VARCHAR(255) COMMENT '腾讯云COS Bucket名称',
    `TENCENT_COS_REGION` VARCHAR(50) COMMENT '腾讯云COS 区域',
    `TENCENT_COS_CDN_DOMAIN` VARCHAR(500) COMMENT '腾讯云COS CDN域名',

    `IS_ACTIVE` CHAR(1) NOT NULL DEFAULT 'Y' COMMENT '是否有效(Y/N)',
    `CREATE_BY` VARCHAR(80) COMMENT '创建人',
    `CREATE_TIME` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `UPDATE_BY` VARCHAR(80) COMMENT '更新人',
    `UPDATE_TIME` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

    UNIQUE KEY `idx_company` (`SYS_COMPANY_ID`),
    INDEX `idx_storage_type` (`STORAGE_TYPE`),
    INDEX `idx_active` (`IS_ACTIVE`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='云盘存储配置表';
```

## 使用方式

### 1. 初始化存储管理器

```go
import (
    "github.com/sky-xhsoft/sky-server/internal/config"
    "github.com/sky-xhsoft/sky-server/internal/pkg/storage"
)

func main() {
    // 加载配置
    cfg, err := config.Load()
    if err != nil {
        log.Fatal(err)
    }

    // 创建存储管理器
    storageManager, err := storage.NewStorageManager(&cfg.Storage)
    if err != nil {
        log.Fatal(err)
    }

    // 获取默认存储
    defaultStorage, err := storageManager.GetDefaultStorage()
    if err != nil {
        log.Fatal(err)
    }
}
```

### 2. 使用公司级存储管理器

```go
import (
    "github.com/sky-xhsoft/sky-server/internal/config"
    "github.com/sky-xhsoft/sky-server/internal/pkg/storage"
    "github.com/sky-xhsoft/sky-server/internal/repository/mysql"
    "gorm.io/gorm"
)

func main() {
    // 初始化DB连接...

    // 加载配置
    cfg, err := config.Load()
    if err != nil {
        log.Fatal(err)
    }

    // 创建配置仓储
    configRepo := mysql.NewCloudStorageConfigRepository(db)

    // 创建公司级存储管理器
    companyStorageManager, err := storage.NewCompanyStorageManager(&cfg.Storage, configRepo, 1)
    if err != nil {
        log.Fatal(err)
    }

    // 获取公司1的存储实例
    company1Storage, err := companyStorageManager.GetStorage(context.Background(), 1)
    if err != nil {
        log.Fatal(err)
    }
}
```

### 3. 在云盘服务中使用

云盘服务已通过依赖注入方式集成了存储管理器：

```go
func NewService(db *gorm.DB, storage storage.Storage) Service {
    return &service{
        db:      db,
        storage: storage,
    }
}
```

## 性能优化

### 1. 配置缓存机制
- 公司配置缓存5分钟TTL
- 避免频繁数据库查询
- 支持手动刷新和清除缓存

### 2. 分块上传支持
- 阿里云OSS和腾讯云COS支持原生分块上传
- 本地存储不支持分块上传（返回错误）
- 分块大小可配置

### 3. 预签名URL
- 支持预签名URL生成，用于前端直传
- 提高上传效率，减少服务器压力

## 安全性考虑

### 1. 配置验证
- 在创建存储实例时验证配置有效性
- 对必填字段进行校验
- 自动处理配置缺失的情况

### 2. 错误处理
- 配置无效时返回默认存储
- 提供详细的错误日志
- 支持自动恢复机制

### 3. 权限控制
- 数据库配置通过公司ID隔离
- 统一的用户认证和权限管理

## 待完善的功能

### 1. 分块上传完善
- 阿里云OSS分块上传支持
- 分块上传进度跟踪
- 分块上传取消和重试

### 2. 存储迁移
- 支持在不同存储之间迁移数据
- 数据备份和恢复机制

### 3. 监控和日志
- 存储操作的详细日志
- 性能指标收集
- 错误告警机制

### 4. 多区域支持
- 存储区域的自动选择
- 跨区域数据同步

## 总结

本实现完成了云盘存储的基础架构，并提供了从数据库读取存储配置的能力。系统具有良好的扩展性和可维护性，支持多种存储方式的灵活切换。通过统一的接口和配置管理，使得存储操作变得简单和可靠。
