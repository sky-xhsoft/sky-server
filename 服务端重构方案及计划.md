# 服务端重构方案及计划

## 项目简介
Sky-Server 是一个用 Go 语言编写的元数据驱动应用框架。系统通过数据库元数据定义来动态生成表单、字段、权限和UI配置。

## 现状分析

### 现有架构
项目已经有完整的分层架构实现：

```
├── api/
│   ├── handler/           # API处理器层
│   ├── middleware/        # 中间件层
│   ├── router/            # 路由层
│   └── swagger/           # Swagger文档
├── cmd/
│   └── server/            # 应用入口
├── internal/
│   ├── config/            # 配置管理（使用viper）
│   ├── model/             # 数据模型
│   ├── pkg/
│   │   ├── cache/         # 缓存工具
│   │   ├── errors/        # 错误处理（AppError）
│   │   ├── executor/      # 执行器
│   │   ├── jwt/           # JWT工具
│   │   ├── logger/        # 日志管理（zap+lumberjack）
│   │   ├── mask/          # 数据脱敏
│   │   ├── permission/    # 权限工具
│   │   ├── storage/       # 存储工具
│   │   ├── tencent/       # 腾讯云工具
│   │   ├── transaction/   # 事务工具
│   │   ├── utils/         # 通用工具（包含响应处理）
│   │   ├── validator/     # 参数验证（空目录）
│   │   └── websocket/     # WebSocket工具
│   ├── repository/        # 仓储层
│   └── service/           # 服务层
└── sqls/                  # SQL脚本
```

### 现有核心组件
| 功能 | 实现位置 | 状态 |
|------|----------|------|
| 配置管理 | `internal/config/` | ✅ 完整 |
| 错误处理 | `internal/pkg/errors/` | ✅ 完整 |
| 日志管理 | `internal/pkg/logger/` | ✅ 完整 |
| 响应处理 | `internal/pkg/utils/response.go` | ✅ 完整 |
| 参数验证 | `internal/pkg/validator/` | ✅ 已实现 |
| 中间件 | `api/middleware/` | ✅ 完整 |
| API路由 | `api/router/` | ✅ 完整 |
| API处理器 | `api/handler/` | ✅ 完整 |
| 服务层 | `internal/service/` | ✅ 已增强 |
| 仓储层 | `internal/repository/` | ✅ 已增强 |
| 领域层 | `internal/domain/` | ✅ 新增 |

## 重构目标
1. 完善缺失的核心组件（参数验证器）
2. 优化现有代码结构和编码规范
3. 添加领域层（domain）结构定义
4. 建立统一的代码规范和最佳实践
5. 提升代码可维护性和可测试性

## 重构阶段

### 第一阶段：完善核心组件（✅ 已完成）
- [x] 分析现有架构
- [x] 实现参数验证器（`internal/pkg/validator/`）
- [x] 完善中间件功能（参数验证、错误处理中间件）
- [ ] 添加代码规范文档

### 第二阶段：API层优化（✅ 已完成）
- [x] 优化API响应处理（支持验证错误格式化）
- [x] 统一错误处理流程（`api/middleware/error_handler.go`）
- [ ] 完善API文档（Swagger）
- [x] 优化参数验证集成（`api/middleware/validation.go`）

### 第三阶段：服务层优化（✅ 已完成）
- [x] 引入依赖注入（`internal/service/container.go`）
- [x] 优化服务接口设计（`internal/service/base_service.go`）
- [x] 添加单元测试框架（`internal/service/test/`）
- [x] 完善事务管理（`internal/pkg/transaction/manager.go`）

### 第四阶段：仓储层优化（✅ 已完成）
- [x] 优化数据库查询（`internal/repository/query.go`）
- [x] 完善缓存策略（`internal/repository/cache.go`）
- [x] 添加Repository接口（`internal/repository/base.go`）
- [x] 实现分页查询（`internal/repository/page.go`）

### 第五阶段：领域层设计（✅ 已完成基础架构）
- [x] 定义领域实体（`internal/domain/base.go`）
- [x] 定义仓储接口（`internal/domain/sso.go`）
- [x] 定义领域服务接口（`internal/domain/sso.go`）
- [x] 实现领域事件机制（`internal/domain/event.go`）

### 第六阶段：性能优化（✅ 已完成基础架构）
- [ ] 优化数据库查询
- [x] 实现缓存机制（`internal/repository/cache.go`）
- [x] 优化请求响应时间（性能监控中间件）
- [x] 完善监控和日志系统（`internal/pkg/monitor/metrics.go`, `api/middleware/metrics.go`）

### 第七阶段：部署和监控
- [ ] 优化部署流程
- [ ] 实现自动化测试
- [x] 完善监控和告警系统（性能监控和健康检查）
- [ ] 实现系统日志分析

## 核心组件说明

### 统一响应格式（现有）
```go
// Response 统一响应结构
type Response struct {
	Code      int         `json:"code"`
	Message   string      `json:"message"`
	Data      interface{} `json:"data,omitempty"`
	Error     interface{} `json:"error,omitempty"`
	Timestamp string      `json:"timestamp"`
}

// 使用方式
utils.Success(c, data)
utils.Error(c, httpStatus, err)
```

### 统一错误处理（现有）
```go
// AppError 应用错误
type AppError struct {
	Code    int    `json:"code"`
	Message string `json:"message"`
	Err     error  `json:"-"`
}

// 使用方式
errors.New(code, message)
errors.Wrap(code, message, err)
```

### 日志管理（现有）
```go
// 初始化
logger.Init(cfg)

// 使用方式
logger.Info("消息", zap.String("key", "value"))
logger.Error("错误", zap.Error(err))
```

### 配置管理（现有）
```go
// 加载配置
cfg, err := config.Load()

// 配置结构
type Config struct {
	App      AppConfig
	Database DatabaseConfig
	Redis    RedisConfig
	JWT      JWTConfig
	Log      LogConfig
	// ...
}
```

## 待完成的核心组件

### 参数验证器（需要实现）
目标：在 `internal/pkg/validator/` 下实现完整的参数验证器

功能要求：
- 支持结构体验证
- 支持自定义验证规则
- 支持中文错误消息
- 集成到现有API处理流程

## 代码规范

### 目录命名规范
- 全小写，使用下划线分隔
- 清晰表达包的用途

### 文件命名规范
- 全小写，使用下划线分隔
- 功能描述性命名

### 函数命名规范
- 导出函数使用驼峰命名
- 内部函数使用小写开头
- 动词开头，清晰表达功能

### 错误处理规范
- 使用 `errors.AppError` 包装业务错误
- 错误码按照分类定义
- 提供清晰的错误消息

## 重构任务清单

### 已完成任务
- [x] 分析现有架构
- [x] 识别核心组件状态
- [x] 实现参数验证器（`internal/pkg/validator/`）
- [x] 创建服务基类（`internal/service/base_service.go`）
- [x] 实现依赖注入容器（`internal/service/container.go`）
- [x] 创建仓储层基础接口（`internal/repository/base.go`）
- [x] 实现查询构建器（`internal/repository/query.go`）
- [x] 实现分页查询接口（`internal/repository/page.go`）
- [x] 创建测试框架和辅助函数（`internal/service/test/`）
- [x] 为SSO服务创建单元测试（`internal/service/sso/sso_service_test.go`）
- [x] 实现领域层基础架构（`internal/domain/`）
- [x] 创建领域事件机制（`internal/domain/event.go`）
- [x] 定义SSO领域服务接口（`internal/domain/sso.go`）
- [x] 完善中间件功能（`api/middleware/validation.go`, `api/middleware/error_handler.go`）
- [x] 完善服务层事务管理（`internal/pkg/transaction/manager.go`）
- [x] 实现仓储层缓存策略（`internal/repository/cache.go`）
- [x] 添加性能监控（`internal/pkg/monitor/metrics.go`, `api/middleware/metrics.go`）
- [x] 集成监控中间件到路由（`api/router/router.go`）
- [x] 添加健康检查接口（`/health`）
- [x] 添加指标接口（`/metrics`）

### 待完成任务
- [ ] 集成参数验证到API处理器
- [ ] 完善API文档（Swagger）
- [ ] 实现自动化测试
- [ ] 完成所有服务的测试覆盖

## 预期效果
1. 完善的核心组件体系
2. 统一的代码规范
3. 更好的代码可维护性
4. 更高的代码测试覆盖率
5. 更清晰的架构层次

## 风险评估
1. **技术风险**：重构时需要保持与现有代码兼容
2. **时间风险**：需要平衡重构与新功能开发
3. **测试风险**：重构后需要进行全面回归测试

## 成功标准
1. 系统功能正常运行
2. 参数验证器完整可用
3. 代码规范统一
4. 测试覆盖率提升
5. 开发效率显著提高
