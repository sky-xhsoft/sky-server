# 生产环境部署信息

## 服务器信息

- **IP 地址**: `119.45.20.166`
- **操作系统**: CentOS 8
- **登录用户**: `root`
- **登录密码**: `Zhoujie613317!`
- **Sky-Server 安装目录**: `/opt/sky-server/`

## 服务信息

- **服务状态**: ✅ 运行中
- **监听端口**: `9090`
- **访问地址**: http://119.45.20.166:9090
- **API 文档**: http://119.45.20.166:9090/swagger/index.html
- **版本**: `v1.0.0`
- **运行模式**: `release`

## 数据库信息

- **数据库类型**: MySQL 8.0
- **数据库地址**: `localhost:3306`
- **数据库名称**: `sky_server`
- **用户名**: `root`
- **密码**: `SkyServer@123`

## Redis 信息

- **地址**: `localhost:6379`
- **密码**: 无
- **DB**: `0`

## 管理命令

```bash
# 查看服务状态
systemctl status sky-server

# 查看实时日志
journalctl -u sky-server -f

# 启动服务
systemctl start sky-server

# 停止服务
systemctl stop sky-server

# 重启服务
systemctl restart sky-server

# 设置开机自启
systemctl enable sky-server

# 禁用开机自启
systemctl disable sky-server
```

## 文件位置

```
/opt/sky-server/
├── bin/
│   └── sky-server          # 可执行文件
├── configs/
│   └── config.yaml         # 配置文件
└── logs/                  # 日志目录（如果配置输出到文件）
```

## 配置文件

配置文件位置: `/opt/sky-server/configs/config.yaml`

编辑配置:
```bash
nano /opt/sky-server/configs/config.yaml
# 编辑后重启服务
systemctl restart sky-server
```

## 当前配置

- **CORS**: 允许所有来源 (`*`)
- **Swagger**: 已启用
- **文件存储**: 本地存储
- **直播功能**: 已禁用（可在配置中启用）
- **Rate Limit**: 已启用，100 请求/分钟

## 部署时间

- **部署完成时间**: 2026-03-26
- **Go 版本**: go1.22.0 linux/amd64

## 备注

- 防火墙已开放 9090 端口（firewalld 未运行，所以默认开放）
- 如需启用腾讯云直播功能，请在配置文件中添加 API 密钥
- 如需启用阿里云 OSS/腾讯云 COS 云存储，请在配置文件中添加 API 密钥
